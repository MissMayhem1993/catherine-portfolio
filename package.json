{
  "name": "catherine-portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio website for Catherine McGraw",
  "main": "index.html",
  "scripts": {
    "build": "sass src/main.scss dist/style.css --no-source-map --style=compressed",
    "watch": "sass src/main.scss dist/style.css --watch"
  },
  "devDependencies": {
    "sass": "^1.90.0"
  }
}
